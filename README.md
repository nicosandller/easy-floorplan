# PR screenshots

Images referenced from pull request descriptions, kept on their own branch so
they render on GitHub without adding binaries to any PR's diff or to `main`.

Nothing here is code and nothing here is merged. Delete the branch once the
pull requests that link these images are closed.

| file | PR |
| --- | --- |
| `pr/240-windows.png` | #240 — fixed windows and partial sashes |
| `pr/241-zoom-level.png`, `pr/241-overlay-scale.png` | #241 — per-room zoom, overlay size |
| `pr/242-*.png` | #242 — lock in place |
| `pr/243-portrait.png`, `pr/243-landscape.png` | #243 — rotation by screen orientation |

Rendered headlessly from the real card and editor against the demo plan in
`docker/config/floorplan-demo.yaml`.
