node_modules/
dist/
*.log
.DS_Store
.claude/
# personal dev deploy script — targets a private HA host
deploy.sh
# never commit secrets — deploy password lives at ~/.ha_floorplan_deploy_secret
*.secret
.env
